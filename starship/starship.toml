# Starship prompt config — managed by ~/dotfiles
# See: https://starship.rs/config/
# Presets: https://starship.rs/presets/

# Add an empty line before each prompt (default: true)
# add_newline = false
